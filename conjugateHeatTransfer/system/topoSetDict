FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      topoSetDict;
}

actions
(
    {
        name    meshgridCells;
        type    cellSet;
        action  new;
        source  pointsToCell;
        points  (#include "meshgrid.csv");
    }
);